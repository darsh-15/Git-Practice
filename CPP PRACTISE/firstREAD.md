This contains the code of doubly linked list in c++. It's has deletion code written in it.



**What are doubly linked list?**

*It's a linear data structure in which each node stores data along the pointers of both next and previous nodes.Unlike a singly linked list, which only points to the next item, a doubly linked list points to 
both.This function enables bidirectional traversal i.e moving in both directions.*


